<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>CV Grégory Loubet-Bonino - Ingénierie</title>
    <style>
        /* --- Styles de base et mise en page --- */
        :root {
            --primary-color: #2c3e50; /* Bleu nuit pro */
            --secondary-color: #34495e;
            --accent-color: #e74c3c; /* Rouge vif pour accents */
            --text-light: #ecf0f1;
            --text-dark: #2c3e50;
            --bg-light: #f4f7f6;
            --border-color: #bdc3c7;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-light);
            color: var(--text-dark);
            line-height: 1.5;
        }

        .cv-container {
            display: flex;
            width: 210mm; /* Largeur A4 */
            height: 297mm; /* Hauteur A4 pour forcer une page */
            margin: 0 auto;
            background: white;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        /* --- Colonne de gauche (Sidebar) --- */
        .sidebar {
            width: 33%;
            background-color: var(--primary-color);
            color: var(--text-light);
            padding: 30px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 4px solid var(--text-light);
            object-fit: cover;
            margin-bottom: 20px;
        }

        .sidebar h1 {
            font-size: 1.4em;
            text-align: center;
            margin: 0 0 10px 0;
            color: var(--text-light);
        }

        .sidebar h2.title {
            font-size: 1em;
            font-weight: normal;
            text-align: center;
            margin: 0 0 30px 0;
            color: #bdc3c7;
        }

        .sidebar-section {
            width: 100%;
            margin-bottom: 25px;
        }

        .sidebar-section h3 {
            font-size: 1.1em;
            border-bottom: 2px solid var(--accent-color);
            padding-bottom: 5px;
            margin-bottom: 15px;
            color: var(--text-light);
        }

        .contact-item, .lang-item {
            font-size: 0.9em;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .contact-item a { color: var(--text-light); text-decoration: none; }

        /* --- Système d'étoiles --- */
        .stars {
            color: #f1c40f; /* Jaune or */
            font-size: 1.2em;
        }
        
        .star-filled::after { content: '★'; }
        .star-empty::after { content: '☆'; color: #bdc3c7; }

        /* --- Colonne de droite (Main) --- */
        .main-content {
            width: 67%;
            padding: 30px;
            overflow-y: auto; /* Permet de scroller si ça dépasse un peu */
        }

        .main-section {
            margin-bottom: 25px;
        }

        .main-section h2 {
            font-size: 1.3em;
            color: var(--primary-color);
            border-bottom: 2px solid var(--border-color);
            padding-bottom: 5px;
            margin-top: 0;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .summary-text {
            font-style: italic;
            color: var(--secondary-color);
            margin-bottom: 20px;
        }

        /* --- Grille de compétences --- */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
        }

        .skill-category h4 {
            margin: 0 0 10px 0;
            color: var(--secondary-color);
            font-size: 1em;
        }

        .skill-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.9em;
            margin-bottom: 5px;
        }

        /* --- Expériences et Projets --- */
        .timeline-item {
            margin-bottom: 15px;
            padding-left: 15px;
            border-left: 2px solid var(--border-color);
            position: relative;
        }
        
        .timeline-item::before {
            content: '';
            position: absolute;
            left: -7px;
            top: 5px;
            width: 12px;
            height: 12px;
            background-color: var(--accent-color);
            border-radius: 50%;
        }

        .item-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
        }

        .item-title { font-weight: bold; color: var(--primary-color); }
        .item-date { font-size: 0.85em; color: #7f8c8d; }
        .item-sub { font-size: 0.9em; font-style: italic; color: var(--secondary-color); margin-bottom: 5px; }

        .item-achievements, .item-details {
            font-size: 0.85em;
            margin: 0;
            padding-left: 15px;
        }

        /* --- Badges et Stack --- */
        .tag-container {
            margin-top: 5px;
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
        }

        .tag {
            font-size: 0.75em;
            padding: 2px 8px;
            border-radius: 12px;
            background-color: #e1e8ed;
            color: var(--primary-color);
        }

        .stack-tag {
            background-color: var(--primary-color);
            color: var(--text-light);
        }

        /* --- Pour l'impression PDF --- */
        @media print {
            body { background: white; }
            .cv-container {
                width: 210mm;
                height: 297mm;
                box-shadow: none;
                -webkit-print-color-adjust: exact; /* Force les couleurs de fond */
                print-color-adjust: exact;
            }
        }
    </style>
</head>
<body>
    <div class="cv-container">
        <div class="sidebar">
            <img src="{{ profile.photo }}" alt="Grégory Loubet-Bonino" class="profile-img">
            <h1>{{ profile.name }}</h1>
            <h2 class="title">{{ profile.title }}</h2>

            <div class="sidebar-section">
                <h3>Contact</h3>
                <div class="contact-item"><i>📍</i> {{ profile.location }}</div>
                <div class="contact-item"><i>📞</i> {{ profile.phone }}</div>
                <div class="contact-item"><i>✉️</i> <a href="mailto:{{ profile.email }}">{{ profile.email }}</a></div>
                <div class="contact-item"><i>🔗</i> <a href="{{ profile.linkedin }}">LinkedIn</a></div>
                <div class="contact-item"><i>💻</i> <a href="{{ profile.github }}">GitHub</a></div>
            </div>

            <div class="sidebar-section">
                <h3>Langues</h3>
                {% for lang in languages %}
                <div class="lang-item">
                    <span>{{ lang.name }}</span>
                    <span class="stars">
                        {# Logique d'étoiles : 5 étoiles max, fluent=5, intermediate=3, basic=1 #}
                        {% if lang.level == "Native" %}<span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span>
                        {% elif lang.level == "Fluent" %}<span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-empty"></span>
                        {% elif lang.level == "Intermediate" %}<span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-empty"></span><span class="star-empty"></span>
                        {% elif lang.level == "Basic" %}<span class="star-filled"></span><span class="star-empty"></span><span class="star-empty"></span><span class="star-empty"></span><span class="star-empty"></span>
                        {% endif %}
                    </span>
                </div>
                {% endfor %}
            </div>
            
            <div class="sidebar-section">
                <h3>Compétences Clés</h3>
                <div class="skill-category">
                    <h4>Programmation</h4>
                    {% for skill in skills_summary.programming %}
                    <div class="skill-item">
                        <span>{{ skill.name }}</span>
                        <span class="stars">
                            {# Logique d'étoiles : Advanced=4, Intermediate=3, Beginer=2 #}
                            {% if skill.level == "Advanced" %}<span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-empty"></span>
                            {% elif skill.level == "Intermediate" %}<span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-empty"></span><span class="star-empty"></span>
                            {% elif skill.level == "Beginer" %}<span class="star-filled"></span><span class="star-filled"></span><span class="star-empty"></span><span class="star-empty"></span><span class="star-empty"></span>
                            {% endif %}
                        </span>
                    </div>
                    {% endfor %}
                </div>
                <div class="skill-category" style="margin-top:10px;">
                    <h4>Électronique & Robotique</h4>
                    {# On pioche manuellement dans les autres catégories #}
                    <div class="skill-item"><span>PCB Design</span><span class="stars"><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span></span></div>
                    <div class="skill-item"><span>ROS2 & Gazebo</span><span class="stars"><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-empty"></span></span></div>
                    <div class="skill-item"><span>Embedded Linux</span><span class="stars"><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-filled"></span><span class="star-empty"></span></span></div>
                </div>
            </div>
        </div>

        <div class="main-content">
            <div class="main-section summary-text">
                {{ profile.summary_engineering }}
            </div>

            <div class="main-section">
                <h2>Expérience Professionnelle</h2>
                {# On filtre pour ne garder que le non-recherche #}
                {% for exp in experience %}
                {% if "Research" not in exp.keywords %}
                <div class="timeline-item">
                    <div class="item-header">
                        <span class="item-title">{{ exp.title }}</span>
                        <span class="item-date">{{ exp.date }}</span>
                    </div>
                    <div class="item-sub">{{ exp.company }}</div>
                    {% if exp.achievements %}
                    <ul class="item-achievements">
                        {% for ach in exp.achievements %} <li>{{ ach }}</li> {% endfor %}
                    </ul>
                    {% endif %}
                    <div class="tag-container stack-tags">
                        {% for tool in exp.stack %}<span class="tag stack-tag">{{ tool }}</span>{% endfor %}
                    </div>
                </div>
                {% endif %}
                {% endfor %}
            </div>

            <div class="main-section">
                <h2>Projets Techniques</h2>
                {# Sélection des projets les plus ingénieurs (robotique & embedded) #}
                {% set selected_projects = [] %}
                {% for proj in projects.robotics %} {% set _ = selected_projects.append(proj) %} {% endfor %}
                {% for proj in projects.embedded_linux %} {% set _ = selected_projects.append(proj) %} {% endfor %}

                {% for proj in selected_projects %}
                <div class="timeline-item">
                    <div class="item-header">
                        <span class="item-title">{{ proj.name }}</span>
                    </div>
                    <div class="item-sub">{{ proj.context }}</div>
                    <p class="item-details" style="font-size: 0.85em; margin: 0 0 5px 0;">{{ proj.details }}</p>
                    {% if proj.achievements %}
                    <ul class="item-achievements">
                        {% for ach in proj.achievements %} <li>{{ ach }}</li> {% endfor %}
                    </ul>
                    {% endif %}
                    <div class="tag-container">
                        {% for kw in proj.keywords %}<span class="tag">{{ kw }}</span>{% endfor %}
                    </div>
                    <div class="tag-container stack-tags">
                        {% for tool in proj.stack %}<span class="tag stack-tag">{{ tool }}</span>{% endfor %}
                    </div>
                </div>
                {% endfor %}
            </div>

            <div class="main-section">
                <h2>Formation</h2>
                {% for edu in education %}
                <div class="timeline-item">
                    <div class="item-header">
                        <span class="item-title">{{ edu.degree }}</span>
                        <span class="item-date">{{ edu.date }}</span>
                    </div>
                    <div class="item-sub">{{ edu.school }}</div>
                    <p class="item-details" style="font-size: 0.85em; margin: 0;">{{ edu.details }}</p>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</body>
</html>